# 3D Models Directory

Place your Oracle Chamber GLB model here:

- `oracle_chamber.glb` - Optional 3D model for the Oracle page

If the GLB file is not present, the Three.js scene will automatically render a fallback glowing orb.

